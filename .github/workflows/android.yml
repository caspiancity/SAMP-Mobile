name: Build SA-MP Mobile APK

on:
  push:
    branches: [ "GTA-2.11", "main" ]
  pull_request:
    branches: [ "GTA-2.11" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Case-Insensitive Workspace
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ciopfs
          mkdir -p /tmp/raw_data /tmp/ci_workspace
          ciopfs /tmp/raw_data /tmp/ci_workspace
          
          # Faylları böyük-kiçik hərf həssaslığı olmayan qovluğa kopyalayırıq
          cp -r . /tmp/ci_workspace/

          # Gradle-in kilid fayllarını ext4 fayl sisteminə yönəldirik
          rm -rf /tmp/ci_workspace/.gradle
          mkdir -p /tmp/real_gradle_locks
          ln -s /tmp/real_gradle_locks /tmp/ci_workspace/.gradle

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK & NDK
        uses: android-actions/setup-android@v3

      - name: Build APK with Gradle
        run: |
          cd /tmp/ci_workspace
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SA-MP-Mobile-Debug
          path: /tmp/ci_workspace/app/build/outputs/apk/debug/*.apk
